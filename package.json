{
  "name": "twingate-github-action",
  "version": "1.0.0",
  "description": "Connect to Twingate network in GitHub Actions",
  "main": "src/index.js",
  "scripts": {
    "build": "ncc build src/index.js -o dist && ncc build src/cleanup.js -o dist2 && mv dist2/index.js dist/cleanup.js && rm -r dist2",
    "package": "npm run build"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/maherapi/github-action.git"
  },
  "keywords": [
    "github-actions",
    "twingate",
    "vpn",
    "networking"
  ],
  "author": "maherapi",
  "license": "MIT",
  "dependencies": {
    "@actions/core": "^1.10.1",
    "@actions/exec": "^1.1.1"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.38.1"
  }
}
